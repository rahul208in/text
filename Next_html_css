
/* General Styles */
.file-item {
  border: 1px solid #ddd;
  margin-bottom: 15px;
  padding: 10px;
  border-radius: 5px;
}

/* Header Section with Button and Link */
.file-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 10px;
}

/* Button Styling */
.toggle-button {
  padding: 8px 12px;
  margin-top: 10px;
  border: 1px solid #ddd;
  border-radius: 4px;
  background-color: #f0f0f0;
  cursor: pointer;
  transition: background-color 0.3s;
}

.toggle-button:hover {
  background-color: #e0e0e0;
}

/* Download Link Styling */
.download-link {
  display: inline-block;
  margin-top: 10px;
  padding: 8px 12px;
  text-decoration: none;
  border: 1px solid #ddd;
  border-radius: 4px;
  background-color: #f0f0f0;
  cursor: pointer;
  transition: background-color 0.3s;
}

.download-link:hover {
  background-color: #e0e0e0;
}

/* Preformatted Content Styling */
.file-content {
  margin-top: 10px;
}

.file-pre {
  background-color: #f0f0f0;
  padding: 10px;
  margin: 10px 0;
  border-radius: 4px;
  overflow: auto;
  max-height: 300px;
  width: 100%;
  box-sizing: border-box;
}
